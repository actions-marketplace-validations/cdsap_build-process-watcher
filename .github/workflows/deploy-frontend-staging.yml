name: Deploy Frontend to Staging

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy from'
        required: true
        default: 'main'
        type: choice
        options:
        - main

jobs:
  deploy-frontend-staging:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.inputs.branch }}

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build application
      run: npm run build

    - name: Create staging config
      run: |
        cat > frontend/public/config.js << EOF
        window.BUILD_PROCESS_WATCHER_CONFIG = {
          backendUrl: '${{ secrets.BACKEND_URL_STAGING }}',
          frontendUrl: '${{ secrets.FRONTEND_URL_STAGING }}',
          environment: 'staging'
        };
        EOF

    - name: Install Firebase CLI
      run: npm install -g firebase-tools

    - name: Deploy to Firebase Staging
      working-directory: ./frontend
      env:
        FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_STAGING }}
        FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID_STAGING }}
      run: |
        echo "$FIREBASE_SERVICE_ACCOUNT" > service-account.json
        export GOOGLE_APPLICATION_CREDENTIALS=service-account.json
        firebase use $FIREBASE_PROJECT_ID
        firebase deploy --only hosting:staging

    - name: Output deployment info
      run: |
        echo "🎨 Staging Frontend deployed successfully!"
        echo "🌐 Staging URL: ${{ secrets.FRONTEND_URL_STAGING }}"
        echo "🔗 Backend API: ${{ secrets.BACKEND_URL_STAGING }}"






